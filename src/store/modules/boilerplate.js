import debug from 'debug';
import * as types from '@/store/types';

const log = debug('app:store/modules/*');

const defaultState = {
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
  state: defaultState,
  getters,
  actions,
  mutations,
};
